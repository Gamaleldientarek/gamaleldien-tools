# Dependencies
node_modules/
.remotion/

# Build artifacts
dist/
build/
*.tar.gz

# Environment
.env
.env.local

# OS
.DS_Store
*.swp

# Worker build output (generated by build-router.py)
worker.js

# Python bytecode (build-router.py)
__pycache__/
*.pyc

# Agent/tooling scratch. Playwright cannot create its output dir inside Google
# Drive (the file provider denies mkdir on dot-directories), so it gets
# symlinked out to /tmp — the link itself must never be committed.
.playwright-mcp
.vercel
.supabase
