# Dependencies
node_modules/
.remotion/

# Build artifacts
dist/
build/
*.tar.gz

# Environment
.env
.env.local

# OS
.DS_Store
*.swp

# Worker build output (generated by build-router.py)
worker.js

# Python bytecode (build-router.py)
__pycache__/
*.pyc

# Scrapped scaffold from the 2026-08-01 random-selector build. It still holds
# LICENSED AzmX and thmanyah font files, which are AZMX client assets and must
# never ship from this repo. Google Drive's file provider denies unlink to the
# shell, so it has to be deleted from Finder — ignored here until that happens.
ZZZ-DELETE-IN-FINDER-random-selector/
