// Empty stub for the `server-only` package so server modules can be imported
// from Vitest (plain Node is a server environment; the real package only
// exists to poison client bundles).
export {};
